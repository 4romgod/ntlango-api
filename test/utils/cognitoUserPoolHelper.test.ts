import {expect} from 'chai';

describe('CognitoUserPoolHelper', () => {
    describe('signUp', () => {
        test('pass test', () => {
            expect(1 + 2).to.deep.equal(3);
        });
    });
});
